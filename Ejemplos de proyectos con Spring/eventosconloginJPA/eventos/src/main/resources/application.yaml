spring:
  application:
    name: Eventos Spring
  datasource:
    url: jdbc:mariadb://localhost/gestion_eventos_login
    username: root
    password: 
    driver-class-name: org.mariadb.jdbc.Driver
  web:
    resources:
      static-locations: file:public
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true