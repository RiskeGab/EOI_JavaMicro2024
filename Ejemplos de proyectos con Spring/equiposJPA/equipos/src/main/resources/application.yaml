spring:
  application:
    name: Equipos Spring
  datasource:
    url: jdbc:mariadb://localhost/equipos
    username: root
    password: 
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true